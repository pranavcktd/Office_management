# URL of the deployed backend API, including the /api prefix, e.g.:
# VITE_API_URL="https://your-backend.up.railway.app/api"
#
# Leave unset for local dev — requests to "/api" are proxied to localhost:4000
# by vite.config.ts's server.proxy.
VITE_API_URL=
